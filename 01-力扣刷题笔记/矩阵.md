# LeedtCode 73. 矩阵置零

##### 解法1：两个标记数组

两个boolean标记数组：row和col
分别记录某一行/列含有0，需要全部置为0，记为true
最后再遍历一遍标记数组，将被标记的位置都设置为0

时间复杂度：O(mn)
空间复杂度：O(m+n)

```java
class Solution {
    public void setZeroes(int[][] matrix) {
        int m = matrix.length;
        int n = matrix[0].length;
  
        boolean[] row = new boolean[m];
        boolean[] col = new boolean[n];

        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++){
                if(matrix[i][j] == 0){
                    row[i] = true;
                    col[j] = true;
                }
            }
        }
        
        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++){
                if(row[i] == true || col[j] == true){
                    matrix[i][j] = 0;
                }
            }
        }
    }
}
```


##### 解法2：两个标记变量
用矩阵的第一行和第一列来代替解法一中的两个标记数组，不用另外开辟空间
用两个标记变量row，col表示矩阵的第一行和第一列是否有0存在，最后处理第一行和第一列的情况
时间复杂度：O(mn)
空间复杂度：O(1)
```java
class Solution {
    public void setZeroes(int[][] matrix) {
        int m = matrix.length;
        int n = matrix[0].length;

        boolean row = false;
        boolean col = false;

        for(int i=0;i<n;i++){
            if(matrix[0][i] == 0){
                row = true;
                break;
            }
        }

        for(int i=0;i<m;i++){
            if(matrix[i][0] == 0){
                col = true;
                break;
            }
        }


        for(int i=1;i<m;i++){
            for(int j=1;j<n;j++){
                if(matrix[i][j] == 0){
                    matrix[0][j] = 0;
                    matrix[i][0] = 0;
                }
            }
        }


        for(int i=1;i<m;i++){
            for(int j=1;j<n;j++){
                if(matrix[i][0] == 0 || matrix[0][j] == 0){
                    matrix[i][j] = 0;
                 }
            }
        }

  
        if(row){
            for(int i=0;i<n;i++){
                matrix[0][i] = 0;
            }
        }
  
        if(col){
            for(int i=0;i<m;i++){
                matrix[i][0] = 0;
            }
        }
    }

}
```

# LeetCode 54. 螺旋矩阵

边界收缩
核心：维护四个边界的循环收缩，并在进行“折返跑”（向左和向上）之前，判断一下是不是已经变成了没有宽度的“单行”或“单列”

# LeetCode 48. 旋转图像

旋转矩阵90度 = 水平对角线翻转 + 主对角线翻转  /  主对角线翻转 + 垂直对角线翻转

# LeetCode 240. 搜索二维矩阵Ⅱ

从右上角开始搜索，可以看作二叉搜索树
时间复杂度：O(m+n)