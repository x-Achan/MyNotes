# 当前阶段任务看板

> 更新日期：2026-04-26
> 当前项目：AI Learning Profile Platform
> 当前阶段：阶段1 — 用户中心基础版
> 阶段目标：完成标准后端项目的基础用户模块，引入 MySQL + MyBatis-Plus + 分层架构

---

## 已完成阶段汇总

### 阶段0：项目初始化与 Spring Boot 入门 ✅

**主要内容**：使用 Spring Initializr 创建项目，编写 /hello 接口，配置 application.yml，初始化 Git 并推送 GitHub，编写 README。

**学习重点**：

| 知识点               | 掌握要点                                           |
| ----------------- | ---------------------------------------------- |
| Spring Initializr | 知道怎么用它创建项目，知道选哪些依赖                             |
| Maven 项目结构        | 知道 pom.xml 管什么，src/main 和 src/test 各放什么        |
| Spring Boot 启动流程  | 知道 main 方法启动后发生了什么（大致流程）                       |
| @RestController   | 知道它 = @Controller + @ResponseBody，标记一个类为接口控制器  |
| @RequestMapping   | 知道它用来映射 URL 路径到方法                              |
| application.yml   | 知道这是 Spring Boot 的配置文件，能配置端口和应用名               |
| Git 基本流程          | 知道 init → add → commit → remote add → push 的流程 |

**验收结果**：全部通过。localhost:8080/hello 正常返回，GitHub 仓库完整，README 清晰。

---

## 阶段1 任务清单

### 任务1：引入 MySQL 并设计 user 表
- [x] 在本地安装并启动 MySQL
- [x] 创建数据库 `learning_profile`
- [x] 设计 `user` 表（id, username, password, nickname, email, role, status, create_time, update_time, is_deleted）
- [x] 在 application.yml 中配置 MySQL 数据源连接信息
- [x] 在 pom.xml 中添加 MySQL 驱动依赖
- **验收**：能在 MySQL 中看到 user 表，Spring Boot 启动日志中数据源连接成功

### 任务2：引入 MyBatis-Plus 并建立分层结构
- [x] 在 pom.xml 中添加 MyBatis-Plus 依赖
- [x] 创建 Entity 层：`User.java`（与 user 表字段对应，使用 MyBatis-Plus 注解）
- [x] 创建 Mapper 层：`UserMapper.java`（继承 BaseMapper）
- [x] 创建 Service 层：`UserService.java` 接口 + `UserServiceImpl.java` 实现（继承 ServiceImpl）
- [x] 创建 Controller 层：`UserController.java`（注入 UserService）
- [x] 理解每层职责：Entity 对应表、Mapper 操作数据库、Service 编写业务逻辑、Controller 处理请求
- **验收**：能用自己的话讲清楚 Entity、Mapper、Service、Controller 各自职责和调用关系

### 任务3：实现用户注册接口
- [ ] 设计注册请求参数（username, password, nickname, email）
- [ ] 在 UserService 中实现注册逻辑（检查用户名是否重复、密码暂明文存储）
- [ ] 在 UserController 中编写 `POST /api/user/register`
- [ ] 使用 Apifox / Postman 测试注册接口
- **验收**：能通过接口注册用户，数据库中能看到新增记录

### 任务4：实现用户信息查询与修改接口
- [ ] 实现 `GET /api/user/{id}` 根据 ID 查询用户
- [ ] 实现 `PUT /api/user/{id}` 修改用户信息（nickname, email）
- [ ] 实现 `DELETE /api/user/{id}` 删除用户（逻辑删除）
- [ ] 使用 Apifox / Postman 测试以上接口
- **验收**：能通过接口完成查、改、删操作，逻辑删除后 is_deleted 字段变化

### 任务5：实现用户列表分页查询接口
- [ ] 实现 `GET /api/user/page` 分页查询用户列表
- [ ] 支持分页参数（page, size）
- [ ] 使用 MyBatis-Plus 的 Page 对象实现分页
- [ ] 配置 MyBatis-Plus 分页插件
- [ ] 使用 Apifox / Postman 测试分页接口
- **验收**：能通过接口获取分页用户列表，返回总数和当前页数据

### 任务6：统一返回结果封装
- [ ] 创建 `Result<T>` 统一返回类（code, message, data）
- [ ] 定义常用静态方法：`success()`, `success(data)`, `error(code, message)`
- [ ] 将所有 Controller 接口返回值改为 `Result<T>`
- [ ] 使用 Apifox / Postman 确认返回格式统一
- **验收**：所有接口返回统一 JSON 格式，包含 code、message、data 字段

### 任务7：全局异常处理
- [ ] 创建 `GlobalExceptionHandler`（@RestControllerAdvice + @ExceptionHandler）
- [ ] 处理参数校验异常（MethodArgumentNotValidException）
- [ ] 处理业务异常（自定义 BusinessException）
- [ ] 处理其他未知异常（Exception）
- [ ] 自定义 BusinessException 类
- **验收**：接口参数不合法或业务出错时，返回统一错误格式，不会抛出 500 堆栈信息

### 任务8：参数校验
- [ ] 在 pom.xml 中添加 spring-boot-starter-validation 依赖
- [ ] 在注册请求参数上添加校验注解（@NotBlank, @Size, @Email 等）
- [ ] 在 Controller 方法参数上添加 @Validated 或 @Valid
- [ ] 测试校验生效：用户名为空、密码过短、邮箱格式错误时应返回明确提示
- **验收**：非法参数请求被拦截，返回具体校验错误信息

### 任务9：实现简单用户登录接口
- [ ] 实现 `POST /api/user/login`（username + password 查询匹配）
- [ ] 登录成功返回用户信息，登录失败返回错误提示
- [ ] 此阶段暂不做 token 和密码加密（阶段2再做）
- **验收**：能通过接口登录，正确账号返回用户信息，错误账号返回提示

---

## 阶段1 整体验收标准

- [ ] 能设计 `user` 表，说出每个字段的作用
- [ ] 能通过接口完成用户注册、查询、修改、删除、分页
- [ ] 能用 Apifox / Postman 独立测试所有接口
- [ ] 能讲清楚项目分层结构：Entity → Mapper → Service → Controller
- [ ] 所有接口返回统一 Result 格式，异常不会暴露堆栈
- [ ] 参数校验生效，非法请求有明确提示

---

## 阶段1 学习重点

| 知识点               | 需要掌握到什么程度                                              |
| ----------------- | ------------------------------------------------------ |
| 分层架构              | 知道 Controller/Service/Mapper/Entity 各自职责，为什么分层，调用关系是什么 |
| MySQL 表设计         | 能设计基础表，理解主键、字段类型、默认值、逻辑删除                              |
| MyBatis-Plus CRUD | 能用 BaseMapper 和 ServiceImpl 完成增删改查和分页查询                |
| MyBatis-Plus 分页插件 | 知道怎么配置分页插件，怎么用 Page 对象                                 |
| 统一返回结果            | 知道为什么需要统一格式，能自己封装 Result 类                             |
| 全局异常处理            | 知道 @RestControllerAdvice 的作用，能处理常见异常类型                 |
| 参数校验              | 知道 @Validated / @Valid 的作用，能使用常用校验注解                   |
| Lombok            | 知道 @Data、@NoArgsConstructor 等注解的作用，能简化 Entity 代码       |
|                   |                                                        |
|                   |                                                        |
|                   |                                                        |

---

## 完成后进入下一阶段

阶段1 完成后，进入**阶段2：登录鉴权版**，届时将引入密码加密（BCrypt）、Token 机制（JWT 或 Sa-Token）、未登录拦截和权限校验。
