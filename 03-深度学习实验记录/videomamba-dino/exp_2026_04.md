
### 21_001

修改 epoch=32

### 22_001
- epoch = 从36 改为 24，学习率衰减区间为lr_drop_epochs: [16, 20]
- num_queries 从300 改为900
- frame_interval从1提升至2，16帧能覆盖更长时间
- weight_decay从0.0001提升至0.001，这能有效限制参数的膨胀，防止模型死记硬背训练集的细枝末节
- 引入了 drop_path_rate: 0.2:（如果你的模型构建代码支持该参数的话，这是原论文中应对过拟合的标准操作）

- 将总 `epochs` 从 `36` 缩短至 **`24`**。既然模型在十几轮就过拟合了，跑 36 轮纯属浪费算力。
- 将 `lr_drop_epochs` 提前到 **`[16, 20]`**。在模型快要陷入死记硬背之前，迅速降低学习率，促使预测框快速收敛。
- 将 `bbox_loss_coef` 和 `cost_bbox` 从 `5.0` 提高到 **`8.0`**。
- 对于极小目标，微小的偏移都会导致 GIoU 为 0，从而产生巨大的梯度干扰。所以我们要告诉模型：“前期先少管形状（GIoU），先把中心点给我死死钉在车上（L1 Bbox）！”


- 修改uavdt.py的__getitem_方法，添加翻转数据增强

#### 22_002

num_frames  16 改为 8
datasets 和 model的 空间分辨率 img_size  改为[[640, 1088]]
frame_interval:从1改为 3 # 🌟 更大的时间跨度（保证运动信息被捕捉）
num_queries 改为 500

修改uavdt.py的
```python
import hashlib  
def idx_for_idx(img_path: Path) -> int:  
    """利用文件全名生成全局唯一的数字 ID"""    hash_str = hashlib.md5(img_path.stem.encode('utf-8')).hexdigest()[:8]  
    return int(hash_str, 16)
```

修改videomamba.py 的 videomamba_tiny
修改train.py的 build_dataloaders
```python
train_dataset = UAVDTDataset(  
    root=cfg['dataset']['data_root'],  
    split='train',  
    num_frames=cfg['dataset']['num_frames'],  
    frame_interval=cfg['dataset'].get('frame_interval', 1),  # 🌟 加回被遗漏的参数  
    sliding_step=cfg['dataset'].get('sliding_step', 1),  # 🌟 强制步长为1，开启密集采样！  
    img_size=tuple(cfg['dataset']['img_size']),  
)  
  
val_dataset = UAVDTDataset(  
    root=cfg['dataset']['data_root'],  
    split='val',  
    num_frames=cfg['dataset']['num_frames'],  
    frame_interval=cfg['dataset'].get('frame_interval', 1),  # 🌟 加回被遗漏的参数  
    sliding_step=cfg['dataset'].get('num_frames', 16),  # 🌟 验证集不重叠，加快评测速度  
    img_size=tuple(cfg['dataset']['img_size']),  
)
```

batch_size = 4
num_wokers = 8
base_lr =  0.0002


修改 train.py 的 load_pretrained_weights